---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<section
    class="border border-slate-800 bg-slate-900/50 backdrop-blur-md w-full max-w-2xl rounded-lg p-6 mb-8"
>
    <h2 class="text-sky-400 font-bold text-xl mb-4">{title}</h2>
    <slot />
</section>

